group: sanity
template: sanity_check

base_config:
  column_types_override: {}
  ignore_columns: []

variants:
  - name: default
    params:
      min_unique_fraction: {type: fixed, value: 0.0}
      max_missing_fraction: {type: float_range, min: 0.7, max: 0.99, step: 0.01, round: 2}
      drop_duplicates: {type: bool}
