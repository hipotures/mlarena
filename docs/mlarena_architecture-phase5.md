- **Zakres**: wdrożyłem strategię testową MLArena z fazy 5: nowa struktura `tests/unit` i `tests/integration`, wspólne fixtury (`conftest.py`) z `ModuleContext`, syntetycznymi danymi i mockiem AutoGluon (`DummyPredictor`), testy jednostkowe dla modułu/pipeline/registry/experiment oraz testy modułów EDA/Model. Przeniosłem scenariusz CLI/pipeline do testów integracyjnych i uzupełniłem o listowanie modułów.
- **Decyzje / odchylenia**: zachowałem istniejące testy spoza MLArena (np. leakage/optuna/kaggle) bez zmian; mock AutoGluon oparty o prosty `DummyPredictor` zamiast pełnej instalacji; profilowanie EDA korzysta z dostępnego `ydata_profiling`, akceptując jego ostrzeżenia deprecation. Legacy e2e (Kaggle, real AG) pozostają sterowane zmiennymi środowiskowymi.
- **Testy**: `pytest tests/unit -q` (17 pass, tylko ostrzeżenia z ydata_profiling); `pytest tests/integration -q` (3 pass, te same ostrzeżenia). Realne e2e (`test_mlarena_kaggle_submit.py`, `test_mlarena_e2e_real.py`) nadal oznaczone skip przez zmienne środowiskowe.
- **Ryzyka / następne kroki**: warto rozważyć filtrację/wyciszenie ostrzeżeń `ydata_profiling` lub podmianę na stub przy braku zależności; dodać pokrycie dla modułów tune/stack/feat po ich rozbudowie; e2e z prawdziwym Kaggle/AutoGluon należy włączyć przed fazą 6 (usunięcie legacy) żeby złapać regresje w realnym środowisku.
- **Artefakty / PR**: nowe pliki `tests/conftest.py`, `tests/unit/**/*`, `tests/integration/**/*`; usunięte stare testy MLArena (`tests/test_mlarena_core.py`, `tests/test_mlarena_cli.py`, `tests/test_mlarena_flow.py`, `tests/test_mlarena_pipeline_validation.py`).
