# Test RFE with RandomForest on Titanic
module: feature_selector
cache: true
config:
  selection_method: rfe
  importance_model_type: rf
  n_features_to_select: 5
  n_estimators: 50
  max_depth: 4
  use_gpu: false  # RF doesn't support GPU
  random_state: 42
