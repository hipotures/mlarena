templates:
  cpu-dev-5m-av:
    model: autogluon_av_weights
    config:
      preset: high
      time_limit: 600
      use_gpu: false
      hyperparameters: {}

  cpu-dev-5m-av-gbm:
    model: autogluon_av_weights
    config:
      preset: high
      time_limit: 600
      use_gpu: false
      hyperparameters:
        included_model_types: ["GBM"]

  cpu-dev-5m-av-xgb:
    model: autogluon_av_weights
    config:
      preset: high
      time_limit: 600
      use_gpu: false
      hyperparameters:
        included_model_types: ["XGB"]

  cpu-dev-5m-av-cat:
    model: autogluon_av_weights
    config:
      preset: high
      time_limit: 600
      use_gpu: false
      hyperparameters:
        included_model_types: ["CAT"]
