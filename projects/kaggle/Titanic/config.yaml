# Titanic Project Configuration
# This file overrides global defaults and can be overridden by profiles or CLI.

common:
  seed: 42
  use_gpu: false

model:
  time_limit: 300
  preset: medium_quality

preprocess:
  cache: true
