# Tail weighting for distribution shift (percentile 0.99, weight_multiplier 4)
module: diabetes_weights
cache: true
config:
  weighting_strategy: percentile
  tail_percentile: 0.99
  weight_multiplier: 4.0
  weights_output_name: sample_weights.csv
  weight_column_name: sample_weight
