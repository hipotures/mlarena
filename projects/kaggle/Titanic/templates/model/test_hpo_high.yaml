# Test template: HPO High preset for Titanic
# Usage: uv run python scripts/mla.py -p Titanic --model-template test_hpo_high

model: autogluon_baseline
hpo_preset: hpo_boost_high
config:
  preset: best
  time_limit: 7200
  use_gpu: false
  included_model_types: [GBM, XGB, CAT]

  # Optional: Override preset defaults
  # num_trials: 150
