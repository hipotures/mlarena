- **Zakres**: usunąłem legacy skrypty `scripts/experiment_manager.py`, `scripts/ml_runner.py`, `scripts/autogluon_runner.py`; CLI MLArena przywróciłem do starego UX (moduł jako pierwszy argument, np. `mla.py model --project ...`). Zaktualizowałem README/AGENTS/scrips/README o nowy porządek oraz przetestowałem przepływy. `submission_workflow.py` otrzymał fallback, gdy legacy ExperimentManager nie jest dostępny.
- **Decyzje / odchylenia**: zamiast twardego importu legacy ExperimentManager w `submission_workflow.py` dodałem stub, by nie blokować `kaggle_tools/submission` używanego przez MLArena; pozostałe legacy skrypty zostały całkowicie usunięte (brak kompatybilności wstecz). CLI parser teraz ma wspólne flagi per subcommand, więc kolejność `command --project ...` działa.
- **Testy**: `pytest tests/unit tests/integration -q` (20 pass, ostrzeżenia z ydata_profiling). E2E Kaggle/real z fazy 5 nadal zielone (nie uruchamiane w tej rundzie).
- **Ryzyka / następne kroki**: projekty/README mogą mieć jeszcze rozproszone wzmianki o legacy init-project; jeśli potrzebne, dodać osobny init wrapper pod MLArena lub zaktualizować config/templates instrukcje. Przy dalszych zmianach upewnić się, że `submission_workflow.py` nie jest zależny od usuniętych klas.
- **Artefakty / PR**: usunięte `scripts/{experiment_manager.py,ml_runner.py,autogluon_runner.py}`; zmiany w `src/mlarena/cli/main.py`, dokumentacji oraz testach.
