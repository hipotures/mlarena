# CatBoost Full Strategy + Cleanup (linear weights v3)
# Pipeline:
# 1. Load external diabetes dataset
# 2. Apply statistical + AI binning
# 3. Map original dataset statistics (mean/count)
# 4. Apply tail weighting (linear v3)
# 5. Remove perfect constants (variance ~ 0)

chain:
  - external-diabetes
  - diabetes-binning
  - diabetes-orig-stats
  - diabetes-tail-weights-linear-v3
  - cleanup-no-variance
