module: av_weights
cache: true
config:
  presets: best
  time_limit: 21600
  drop_columns:
  - id
  included_model_types:
  - CAT
  - GBM
  - XGB
  output_path: train_av_weights.csv
  model_output_dir: av_model
